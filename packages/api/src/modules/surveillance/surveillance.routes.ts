import { FastifyInstance } from 'fastify'

/**
 * Disease Surveillance routes - DISABLED
 * All functionality disabled until enum type issues resolved
 */
export async function surveillanceRoutes(fastify: FastifyInstance) {
  // DISABLED
  return
}
